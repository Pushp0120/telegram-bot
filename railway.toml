[build]
builder = "nixpacks"

[deploy]
startCommand = "python app_updated.py"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[env]
PORT = "7860"
